Branches: `staging` for development, `main` triggers GitHub Pages deployment.

Never force-push to main.

Work on staging or feature branches. Merge to main only when ready to deploy.

Commit message prefixes: feat:, fix:, docs:, style:, chore:, refactor:
